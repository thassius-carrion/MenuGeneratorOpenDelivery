describe("Schema Validator - Functional Tests", () => {
    let cyButtonReturn: string = '[data-cy="btn-return"]'

    beforeEach(() => {
        cy.visit(Cypress.env('baseUrl'))
    });

    it("Navigate to root path", () => {
        
    })
})